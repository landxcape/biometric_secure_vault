name: biometric_secure_vault
description: "Secure key-value storage gated by biometric authentication with change detection and PIN fallback."
version: 1.2.1
homepage: https://github.com/landxcape/biometric_secure_vault
repository: https://github.com/landxcape/biometric_secure_vault
issue_tracker: https://github.com/landxcape/biometric_secure_vault/issues

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  local_auth: ^3.0.1
  flutter_secure_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:
